# 首次交付说明

课程源目录：`/tmp/build-learning-course-forward-20260917/calculus`。

- 课程首页：http://127.0.0.1:4317/
- 完整路线：http://127.0.0.1:4317/OUTLINE.html
- 标杆讲义：http://127.0.0.1:4317/03-derivative-at-a-point/
- 操作网页：http://127.0.0.1:4317/labs/03-derivative-at-a-point/index.html
- 静态构建目录：`.vuepress/dist`。

已交付 8 周 × 每周 3 小时的完整、有边界路线，以及第 03 章完整标杆。标杆内补足平均变化率与极限的必要前置，包含同一例子的逐步推导、方法对照、绝对值尖点、6 道自测及有解释的答案。网页允许修改观察点与正负增量、切换三个函数、显示/隐藏切线，并提供错误反馈与恢复。Node 复算与网页共用数学逻辑。

当前预览进程已在 4317 启动。若进程已停止，在课程目录运行 `SITE_PORT=4317 npm run preview`；重新生成静态内容先运行 `npm run build`。

实际验证：依赖安装、结构检查、默认 Node 实验（25 项手算值/边界断言）、自定义 Node 输入、开发服务、静态构建均成功。最终静态预览在 Chromium 上完成 10/10 组浏览器检查，包括正文搜索跳转、9 处数学公式、答案折叠、真实参数变化、边界与恢复、390px 窄屏及静态 404。完整范围和具体证据见 `reviews/03-derivative-at-a-point.md`，机器结果见 `.course-checks/report.json` 和 `reviews/browser-validation.json`。

材料状态：第 03 章 verified，其他 7 章 planned。**标杆校准仍为 pending；尚无真实用户反馈或作答，学习者掌握状态未评估。** 按本次请求停在首次交付，不联系真实用户、不继续生成其余讲义。

本轮障碍与修订：Markdown 表格的绝对值符号已转义；避免将线性函数的精确预测误称为有误差的近似；第一轮浏览器检查的两个错误假设已修正并保留失败证据；同步技能维护方通知的三个 CLI 入口脚本和默认待准备章节 CSS 修复后完成最终构建与浏览器复验。没有编辑技能包，也没有声称复现本课未触发的模板旧故障。

未验证：Safari/Firefox、真机触屏、屏幕阅读器、深色主题、所有数值、子目录部署、公网发布、另一台电脑的干净安装以及真实学习效果。它们没有被写成通过。
