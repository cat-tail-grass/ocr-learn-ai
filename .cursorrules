# OCR 学习项目 - AI 生成规则

## 项目目标

这是一个 OCR（光学字符识别）底层原理学习项目，最终目标是**完全自主开发**一个可用的 OCR 工具库，不依赖任何第三方 OCR 引擎。

- **实现语言**：JavaScript
- **学习方式**：逐个知识点学习，每个知识点包含代码示例和详细讲解
- **最终目标**：构建一个完整的、自主可控的 OCR 引擎
- **参考文档**：`OUTLINE.md`（大纲）、`PROGRESS.md`（进度）

---

## 代码生成规范

### 1. 生成原则

- **逐步生成**：每次只生成一个知识点的代码，不要一次性生成多个知识点
- **深入浅出**：代码示例要易于理解，从简单到复杂递进
- **注释详尽**：所有代码必须包含详细的中文注释，解释原理和作用
- **可运行性**：生成的代码必须能够直接运行，不能有语法错误
- **可复用性**：代码要模块化，便于最终整合到 OCR 引擎中

### 2. 代码注释要求

```javascript
/**
 * 函数名称：functionName
 * 功能说明：详细说明这个函数做什么
 * 
 * 原理解释：
 * - 解释背后的数学原理或算法思想
 * - 说明为什么要这样实现
 * 
 * @param {类型} 参数名 - 参数说明
 * @returns {类型} 返回值说明
 */
function functionName(param) {
    // 具体步骤注释
    // Step 1: 做什么
    
    // Step 2: 做什么
}
```

### 3. 文件结构规范

每个知识点文件夹应包含：

```
XX-knowledge-point/
├── README.md          # 知识点说明文档（必须）
├── index.js           # 主要示例代码（Node.js 环境）
├── index.html         # 浏览器演示页面（如适用）
├── examples/          # 额外的示例（如有需要）
└── assets/            # 测试图片等资源（如有需要）
```

### 4. 代码风格

- 使用 ES6+ 语法
- 变量命名使用驼峰命名法（camelCase）
- 常量使用全大写下划线命名（UPPER_SNAKE_CASE）
- 函数要有明确的单一职责
- 避免过长的函数，适当拆分

---

## 进度文档维护规则

### 每次生成代码后必须更新 PROGRESS.md

更新内容包括：

1. **当前状态**：更新阶段、完成度
2. **已完成知识点**：添加新完成的知识点记录
3. **下一步学习**：更新为下一个知识点的预告和建议
4. **知识点依赖关系**：说明当前知识点与后续的关联

### 更新格式示例

```markdown
### XX. 知识点名称 ✅
- 完成日期：YYYY-MM-DD
- 核心收获：
  1. 学会了 xxx
  2. 理解了 xxx 原理
  3. 能够实现 xxx
- 关键代码文件：`XX-folder/xxx.js`
- 可复用模块：（说明哪些代码可以用于最终OCR引擎）
- 与下一知识点的关联：xxx
```

---

## 知识点连贯性要求

### 生成新知识点前必须：

1. **读取 PROGRESS.md**：了解当前进度和已完成的知识点
2. **读取 OUTLINE.md**：了解当前知识点的详细信息和学习目标
3. **复用已有代码**：确保新代码能复用之前知识点的工具函数
4. **说明关联**：在代码注释中说明与前置知识点的关联

### 代码复用原则

- 通用工具函数放在 `shared/` 并且按照章节 知识点 目录结构组织存放，不要乱放。
- 并且工具函数要添加基于Jest的单元测试，确保工具函数的功能正确性，在更新PROGRESS.md之前必须对单元测试进行自我校验，确保单元测试通过。
- 新知识点可以引用之前知识点的代码
- 避免重复实现相同功能
- 保持代码的模块化和可复用性
- **所有模块最终要整合到 25-ocr-engine 中**

---

## 自主OCR引擎开发要求

### 核心原则

1. **不使用第三方OCR库**：如 Tesseract.js、paddle-ocr 等
2. **可使用基础工具库**：如 Canvas API、TensorFlow.js（用于实现自己的模型）
3. **完全理解每一行代码**：不使用黑盒组件

### 最终交付物

1. **完整的自主OCR工具库源码**
2. **模块化架构**：每个模块可独立使用
3. **API文档与使用说明**
4. **性能测试报告**
5. **真实场景验证结果**

### OCR引擎架构目标

```
OCREngine
├── Preprocessor（预处理模块）
├── Detector（检测模块）
├── Recognizer（识别模块）
├── PostProcessor（后处理模块）
└── recognize(image) → text
```

---

## 语言规范

- **注释和文档**：使用中文
- **代码变量/函数名**：使用英文
- **专业术语**：保留英文，括号内注明中文
  - 例如：`Convolution（卷积）`、`Threshold（阈值）`

---

## 特别注意事项

1. **用户节奏**：用户会在完全掌握当前知识点后才要求生成下一个
2. **不要跳过**：严格按照 OUTLINE.md 中的顺序，不要跳过任何知识点
3. **不要提前引入**：不要提前使用后续知识点的概念
4. **保持耐心**：按用户节奏推进，不要着急
5. **面向最终目标**：每个知识点都要考虑如何整合到最终的OCR引擎中

---

## 知识点列表速查

| 阶段 | 编号 | 知识点 |
|------|------|--------|
| 基础准备 | 01 | 数字图像基础 |
| 基础准备 | 02 | JavaScript 图像处理基础 |
| 图像预处理 | 03 | 灰度化 |
| 图像预处理 | 04 | 二值化 |
| 图像预处理 | 05 | 图像去噪 |
| 图像预处理 | 06 | 形态学操作 |
| 图像预处理 | 07 | 倾斜校正 |
| 文本检测 | 08 | 边缘检测 |
| 文本检测 | 09 | 连通域分析 |
| 文本检测 | 10 | 文本区域定位 |
| 传统识别 | 11 | 特征提取 |
| 传统识别 | 12 | 模板匹配 |
| 传统识别 | 13 | KNN 分类器 |
| 深度学习基础 | 14 | 神经网络基础 |
| 深度学习基础 | 15 | 卷积神经网络 |
| 深度学习基础 | 16 | TensorFlow.js 入门 |
| 深度学习OCR | 17 | CNN 字符分类器 |
| 深度学习OCR | 18 | 循环神经网络基础 |
| 深度学习OCR | 19 | CTC Loss |
| 深度学习OCR | 20 | CRNN 架构 |
| 深度学习OCR | 21 | Attention 机制 |
| 现代OCR | 22 | 文本检测网络 |
| 现代OCR | 23 | Transformer 与现代 OCR |
| 自主引擎开发 | 24 | 后处理与优化 |
| 自主引擎开发 | 25 | 完整OCR引擎开发 |
| 自主引擎开发 | 26 | 实战验证与优化 |
